{
  "name": "sabri-homeopathy/cf-03-financial-operations",
  "description": "Founder-owned voluntary one-time donations, financial transparency, immutable ledger, refunds, reconciliation and fail-closed financial operations for the Sabri Social Homeopathy Platform.",
  "type": "wordpress-plugin",
  "license": "GPL-2.0-or-later",
  "require": {"php": ">=8.1"},
  "autoload": {"psr-4": {"Sabri\\CF03\\": "src/"}},
  "scripts": {
    "test": [
      "php tests/run-new-governing-plans.php",
      "php tests/run-new-governing-plans-adversarial.php"
    ],
    "test:historical": [
      "@php -r \"fwrite(STDOUT, 'Historical suites remain for provenance only and encode superseded business rules.\\n');\""
    ],
    "lint": "find . -name '*.php' -not -path './vendor/*' -print0 | xargs -0 -n1 php -l"
  }
}
