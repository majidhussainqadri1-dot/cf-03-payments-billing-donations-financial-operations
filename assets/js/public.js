(()=>{'use strict';const cfg=window.SabriCF03||{};const request=async(path,options={})=>{const headers={'Content-Type':'application/json',...(options.headers||{})};if(cfg.nonce)headers['X-WP-Nonce']=cfg.nonce;const response=await fetch((cfg.root||'')+path,{credentials:'same-origin',...options,headers});const body=await response.json().catch(()=>({message:cfg.messages?.failed||'Request failed'}));if(!response.ok)throw new Error(body.message||body.code||cfg.messages?.failed||'Request failed');return body};const key=()=>{const bytes=new Uint8Array(24);crypto.getRandomValues(bytes);return 'web-'+Array.from(bytes,b=>b.toString(16).padStart(2,'0')).join('')};document.addEventListener('submit',async event=>{const form=event.target.closest('.sabri-cf03-donation-form');if(!form)return;event.preventDefault();const status=form.querySelector('.sabri-cf03-status');status.textContent=cfg.messages?.working||'Working…';try{const data=new FormData(form);const selected=data.get('amount_minor');const custom=Number(data.get('custom_amount'));const minor=custom>0?Math.round(custom*100):Number(selected);if(!Number.isSafeInteger(minor)||minor<=0)throw new Error('Choose or enter a valid positive amount.');const idempotency=key();form.elements.idempotency_key.value=idempotency;const result=await request('donation-intents',{method:'POST',body:JSON.stringify({amount_minor:minor,currency:'USD',monthly:data.get('monthly')==='1',monthly_consent:data.get('monthly')==='1',idempotency_key:idempotency})});if(result.hosted_url){window.location.assign(result.hosted_url);return;}status.textContent=result.message||result.status||'Request recorded.';}catch(error){status.textContent=error instanceof Error?error.message:(cfg.messages?.failed||'Request failed');}});document.addEventListener('click',async event=>{const button=event.target.closest('[data-sabri-cf03-load-billing]');if(!button)return;const root=button.closest('.sabri-cf03-billing');const target=root?.querySelector('.sabri-cf03-billing-results');if(!target)return;button.disabled=true;target.textContent=cfg.messages?.working||'Working…';try{const result=await request('billing');target.textContent='';for(const [group,records] of Object.entries(result)){if(!Array.isArray(records))continue;const heading=document.createElement('h3');heading.textContent=group.replaceAll('_',' ');target.append(heading);for(const record of records){const article=document.createElement('article');article.className='sabri-cf03-record';const dl=document.createElement('dl');for(const [name,value] of Object.entries(record)){const dt=document.createElement('dt');dt.textContent=name.replaceAll('_',' ');const dd=document.createElement('dd');dd.textContent=value==null?'—':String(value);dl.append(dt,dd)}article.append(dl);target.append(article)}}}catch(error){target.textContent=error instanceof Error?error.message:(cfg.messages?.failed||'Request failed')}finally{button.disabled=false}})})();
